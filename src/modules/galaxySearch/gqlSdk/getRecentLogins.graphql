query getRecentLogins($durationSeconds: Int, $limit: Int, $offset: Int) {
  recentLogins(
    durationSeconds: $durationSeconds
    limit: $limit
    offset: $offset
  ) {
    totalResults
    results {
      id
      account {
        characters {
          id
        }
      }
    }
  }
}
