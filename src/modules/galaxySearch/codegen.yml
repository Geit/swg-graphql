overwrite: true
schema: "../../graphql-schema.generated.json"
documents:
  - '**/*.graphql'
generates:
  ./gqlSdk/graphql.generated.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
