overwrite: true
schema: "http://localhost:4000/graphql"
documents:
  - '**/*.graphql'
generates:
  ./gqlSdk/graphql.generated.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
